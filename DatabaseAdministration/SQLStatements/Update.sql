-- Update Operation (Updating existing Table)

		UPDATE Employees
		SET Department= 'IT'
		WHERE EmployeeID=2;

		UPDATE Employees
		SET Age='30'
		WHERE FirstName='Mary';

		SELECT*
		FROM Employees;
